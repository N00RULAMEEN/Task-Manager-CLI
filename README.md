# Task-Manager-CLI
Task Manager CLI is a Python command-line application that helps users manage daily tasks directly from the terminal. It supports adding, deleting, filtering, and completing tasks with features like priority levels, due dates, overdue detection, progress tracking, and JSON-based persistent storage.
